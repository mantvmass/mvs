// GENERATED by scripts/gen_ops_diff.py: every operator on u8
// The C file next to it is the reference; difftest.sh requires identical output.
import { io } from "std";

func main() -> i8 {
    {
        let a: u8 = 255;
        let b: u8 = 1;
        let r_add: u8 = a + b; io.out("{}", r_add);
        let r_sub: u8 = a - b; io.out("{}", r_sub);
        let r_mul: u8 = a * b; io.out("{}", r_mul);
        let r_and: u8 = a & b; io.out("{}", r_and);
        let r_or: u8 = a | b; io.out("{}", r_or);
        let r_xor: u8 = a ^ b; io.out("{}", r_xor);
        let r_div: u8 = a / b; io.out("{}", r_div);
        let r_rem: u8 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: u8 = a << 1; io.out("{}", r_shl1);
        let r_shr1: u8 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: u8 = a << 3; io.out("{}", r_shl3);
        let r_shr3: u8 = a >> 3; io.out("{}", r_shr3);
        let r_shl7: u8 = a << 7; io.out("{}", r_shl7);
        let r_shr7: u8 = a >> 7; io.out("{}", r_shr7);
        let r_not: u8 = ~a; io.out("{}", r_not);
    }
    {
        let a: u8 = 0;
        let b: u8 = 5;
        let r_add: u8 = a + b; io.out("{}", r_add);
        let r_sub: u8 = a - b; io.out("{}", r_sub);
        let r_mul: u8 = a * b; io.out("{}", r_mul);
        let r_and: u8 = a & b; io.out("{}", r_and);
        let r_or: u8 = a | b; io.out("{}", r_or);
        let r_xor: u8 = a ^ b; io.out("{}", r_xor);
        let r_div: u8 = a / b; io.out("{}", r_div);
        let r_rem: u8 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: u8 = a << 1; io.out("{}", r_shl1);
        let r_shr1: u8 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: u8 = a << 3; io.out("{}", r_shl3);
        let r_shr3: u8 = a >> 3; io.out("{}", r_shr3);
        let r_shl7: u8 = a << 7; io.out("{}", r_shl7);
        let r_shr7: u8 = a >> 7; io.out("{}", r_shr7);
        let r_not: u8 = ~a; io.out("{}", r_not);
    }
    {
        let a: u8 = 255;
        let b: u8 = 3;
        let r_add: u8 = a + b; io.out("{}", r_add);
        let r_sub: u8 = a - b; io.out("{}", r_sub);
        let r_mul: u8 = a * b; io.out("{}", r_mul);
        let r_and: u8 = a & b; io.out("{}", r_and);
        let r_or: u8 = a | b; io.out("{}", r_or);
        let r_xor: u8 = a ^ b; io.out("{}", r_xor);
        let r_div: u8 = a / b; io.out("{}", r_div);
        let r_rem: u8 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: u8 = a << 1; io.out("{}", r_shl1);
        let r_shr1: u8 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: u8 = a << 3; io.out("{}", r_shl3);
        let r_shr3: u8 = a >> 3; io.out("{}", r_shr3);
        let r_shl7: u8 = a << 7; io.out("{}", r_shl7);
        let r_shr7: u8 = a >> 7; io.out("{}", r_shr7);
        let r_not: u8 = ~a; io.out("{}", r_not);
    }
    {
        let a: u8 = 130;
        let b: u8 = 9;
        let r_add: u8 = a + b; io.out("{}", r_add);
        let r_sub: u8 = a - b; io.out("{}", r_sub);
        let r_mul: u8 = a * b; io.out("{}", r_mul);
        let r_and: u8 = a & b; io.out("{}", r_and);
        let r_or: u8 = a | b; io.out("{}", r_or);
        let r_xor: u8 = a ^ b; io.out("{}", r_xor);
        let r_div: u8 = a / b; io.out("{}", r_div);
        let r_rem: u8 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: u8 = a << 1; io.out("{}", r_shl1);
        let r_shr1: u8 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: u8 = a << 3; io.out("{}", r_shl3);
        let r_shr3: u8 = a >> 3; io.out("{}", r_shr3);
        let r_shl7: u8 = a << 7; io.out("{}", r_shl7);
        let r_shr7: u8 = a >> 7; io.out("{}", r_shr7);
        let r_not: u8 = ~a; io.out("{}", r_not);
    }
    {
        let a: u8 = 7;
        let b: u8 = 3;
        let r_add: u8 = a + b; io.out("{}", r_add);
        let r_sub: u8 = a - b; io.out("{}", r_sub);
        let r_mul: u8 = a * b; io.out("{}", r_mul);
        let r_and: u8 = a & b; io.out("{}", r_and);
        let r_or: u8 = a | b; io.out("{}", r_or);
        let r_xor: u8 = a ^ b; io.out("{}", r_xor);
        let r_div: u8 = a / b; io.out("{}", r_div);
        let r_rem: u8 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: u8 = a << 1; io.out("{}", r_shl1);
        let r_shr1: u8 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: u8 = a << 3; io.out("{}", r_shl3);
        let r_shr3: u8 = a >> 3; io.out("{}", r_shr3);
        let r_shl7: u8 = a << 7; io.out("{}", r_shl7);
        let r_shr7: u8 = a >> 7; io.out("{}", r_shr7);
        let r_not: u8 = ~a; io.out("{}", r_not);
    }
    {
        let a: u8 = 200;
        let b: u8 = 7;
        let r_add: u8 = a + b; io.out("{}", r_add);
        let r_sub: u8 = a - b; io.out("{}", r_sub);
        let r_mul: u8 = a * b; io.out("{}", r_mul);
        let r_and: u8 = a & b; io.out("{}", r_and);
        let r_or: u8 = a | b; io.out("{}", r_or);
        let r_xor: u8 = a ^ b; io.out("{}", r_xor);
        let r_div: u8 = a / b; io.out("{}", r_div);
        let r_rem: u8 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: u8 = a << 1; io.out("{}", r_shl1);
        let r_shr1: u8 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: u8 = a << 3; io.out("{}", r_shl3);
        let r_shr3: u8 = a >> 3; io.out("{}", r_shr3);
        let r_shl7: u8 = a << 7; io.out("{}", r_shl7);
        let r_shr7: u8 = a >> 7; io.out("{}", r_shr7);
        let r_not: u8 = ~a; io.out("{}", r_not);
    }
    return 0;
}
