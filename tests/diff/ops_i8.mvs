// GENERATED by scripts/gen_ops_diff.py: every operator on i8
// The C file next to it is the reference; difftest.sh requires identical output.
import { io } from "std";

func main() -> i8 {
    {
        let a: i8 = 127;
        let b: i8 = 1;
        let r_add: i8 = a + b; io.out("{}", r_add);
        let r_sub: i8 = a - b; io.out("{}", r_sub);
        let r_mul: i8 = a * b; io.out("{}", r_mul);
        let r_and: i8 = a & b; io.out("{}", r_and);
        let r_or: i8 = a | b; io.out("{}", r_or);
        let r_xor: i8 = a ^ b; io.out("{}", r_xor);
        let r_div: i8 = a / b; io.out("{}", r_div);
        let r_rem: i8 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: i8 = a << 1; io.out("{}", r_shl1);
        let r_shr1: i8 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: i8 = a << 3; io.out("{}", r_shl3);
        let r_shr3: i8 = a >> 3; io.out("{}", r_shr3);
        let r_shl7: i8 = a << 7; io.out("{}", r_shl7);
        let r_shr7: i8 = a >> 7; io.out("{}", r_shr7);
        let r_not: i8 = ~a; io.out("{}", r_not);
        let r_neg: i8 = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: i8 = -128;
        let b: i8 = 1;
        let r_add: i8 = a + b; io.out("{}", r_add);
        let r_sub: i8 = a - b; io.out("{}", r_sub);
        let r_mul: i8 = a * b; io.out("{}", r_mul);
        let r_and: i8 = a & b; io.out("{}", r_and);
        let r_or: i8 = a | b; io.out("{}", r_or);
        let r_xor: i8 = a ^ b; io.out("{}", r_xor);
        let r_div: i8 = a / b; io.out("{}", r_div);
        let r_rem: i8 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_not: i8 = ~a; io.out("{}", r_not);
        let r_neg: i8 = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: i8 = 127;
        let b: i8 = 3;
        let r_add: i8 = a + b; io.out("{}", r_add);
        let r_sub: i8 = a - b; io.out("{}", r_sub);
        let r_mul: i8 = a * b; io.out("{}", r_mul);
        let r_and: i8 = a & b; io.out("{}", r_and);
        let r_or: i8 = a | b; io.out("{}", r_or);
        let r_xor: i8 = a ^ b; io.out("{}", r_xor);
        let r_div: i8 = a / b; io.out("{}", r_div);
        let r_rem: i8 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: i8 = a << 1; io.out("{}", r_shl1);
        let r_shr1: i8 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: i8 = a << 3; io.out("{}", r_shl3);
        let r_shr3: i8 = a >> 3; io.out("{}", r_shr3);
        let r_shl7: i8 = a << 7; io.out("{}", r_shl7);
        let r_shr7: i8 = a >> 7; io.out("{}", r_shr7);
        let r_not: i8 = ~a; io.out("{}", r_not);
        let r_neg: i8 = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: i8 = -128;
        let b: i8 = 7;
        let r_add: i8 = a + b; io.out("{}", r_add);
        let r_sub: i8 = a - b; io.out("{}", r_sub);
        let r_mul: i8 = a * b; io.out("{}", r_mul);
        let r_and: i8 = a & b; io.out("{}", r_and);
        let r_or: i8 = a | b; io.out("{}", r_or);
        let r_xor: i8 = a ^ b; io.out("{}", r_xor);
        let r_div: i8 = a / b; io.out("{}", r_div);
        let r_rem: i8 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_not: i8 = ~a; io.out("{}", r_not);
        let r_neg: i8 = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: i8 = 0;
        let b: i8 = 5;
        let r_add: i8 = a + b; io.out("{}", r_add);
        let r_sub: i8 = a - b; io.out("{}", r_sub);
        let r_mul: i8 = a * b; io.out("{}", r_mul);
        let r_and: i8 = a & b; io.out("{}", r_and);
        let r_or: i8 = a | b; io.out("{}", r_or);
        let r_xor: i8 = a ^ b; io.out("{}", r_xor);
        let r_div: i8 = a / b; io.out("{}", r_div);
        let r_rem: i8 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: i8 = a << 1; io.out("{}", r_shl1);
        let r_shr1: i8 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: i8 = a << 3; io.out("{}", r_shl3);
        let r_shr3: i8 = a >> 3; io.out("{}", r_shr3);
        let r_shl7: i8 = a << 7; io.out("{}", r_shl7);
        let r_shr7: i8 = a >> 7; io.out("{}", r_shr7);
        let r_not: i8 = ~a; io.out("{}", r_not);
        let r_neg: i8 = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: i8 = -1;
        let b: i8 = -1;
        let r_add: i8 = a + b; io.out("{}", r_add);
        let r_sub: i8 = a - b; io.out("{}", r_sub);
        let r_mul: i8 = a * b; io.out("{}", r_mul);
        let r_and: i8 = a & b; io.out("{}", r_and);
        let r_or: i8 = a | b; io.out("{}", r_or);
        let r_xor: i8 = a ^ b; io.out("{}", r_xor);
        let r_div: i8 = a / b; io.out("{}", r_div);
        let r_rem: i8 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_not: i8 = ~a; io.out("{}", r_not);
        let r_neg: i8 = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: i8 = 7;
        let b: i8 = 3;
        let r_add: i8 = a + b; io.out("{}", r_add);
        let r_sub: i8 = a - b; io.out("{}", r_sub);
        let r_mul: i8 = a * b; io.out("{}", r_mul);
        let r_and: i8 = a & b; io.out("{}", r_and);
        let r_or: i8 = a | b; io.out("{}", r_or);
        let r_xor: i8 = a ^ b; io.out("{}", r_xor);
        let r_div: i8 = a / b; io.out("{}", r_div);
        let r_rem: i8 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: i8 = a << 1; io.out("{}", r_shl1);
        let r_shr1: i8 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: i8 = a << 3; io.out("{}", r_shl3);
        let r_shr3: i8 = a >> 3; io.out("{}", r_shr3);
        let r_shl7: i8 = a << 7; io.out("{}", r_shl7);
        let r_shr7: i8 = a >> 7; io.out("{}", r_shr7);
        let r_not: i8 = ~a; io.out("{}", r_not);
        let r_neg: i8 = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: i8 = -7;
        let b: i8 = 3;
        let r_add: i8 = a + b; io.out("{}", r_add);
        let r_sub: i8 = a - b; io.out("{}", r_sub);
        let r_mul: i8 = a * b; io.out("{}", r_mul);
        let r_and: i8 = a & b; io.out("{}", r_and);
        let r_or: i8 = a | b; io.out("{}", r_or);
        let r_xor: i8 = a ^ b; io.out("{}", r_xor);
        let r_div: i8 = a / b; io.out("{}", r_div);
        let r_rem: i8 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_not: i8 = ~a; io.out("{}", r_not);
        let r_neg: i8 = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: i8 = 7;
        let b: i8 = -3;
        let r_add: i8 = a + b; io.out("{}", r_add);
        let r_sub: i8 = a - b; io.out("{}", r_sub);
        let r_mul: i8 = a * b; io.out("{}", r_mul);
        let r_and: i8 = a & b; io.out("{}", r_and);
        let r_or: i8 = a | b; io.out("{}", r_or);
        let r_xor: i8 = a ^ b; io.out("{}", r_xor);
        let r_div: i8 = a / b; io.out("{}", r_div);
        let r_rem: i8 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: i8 = a << 1; io.out("{}", r_shl1);
        let r_shr1: i8 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: i8 = a << 3; io.out("{}", r_shl3);
        let r_shr3: i8 = a >> 3; io.out("{}", r_shr3);
        let r_shl7: i8 = a << 7; io.out("{}", r_shl7);
        let r_shr7: i8 = a >> 7; io.out("{}", r_shr7);
        let r_not: i8 = ~a; io.out("{}", r_not);
        let r_neg: i8 = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: i8 = -7;
        let b: i8 = -3;
        let r_add: i8 = a + b; io.out("{}", r_add);
        let r_sub: i8 = a - b; io.out("{}", r_sub);
        let r_mul: i8 = a * b; io.out("{}", r_mul);
        let r_and: i8 = a & b; io.out("{}", r_and);
        let r_or: i8 = a | b; io.out("{}", r_or);
        let r_xor: i8 = a ^ b; io.out("{}", r_xor);
        let r_div: i8 = a / b; io.out("{}", r_div);
        let r_rem: i8 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_not: i8 = ~a; io.out("{}", r_not);
        let r_neg: i8 = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: i8 = 66;
        let b: i8 = 9;
        let r_add: i8 = a + b; io.out("{}", r_add);
        let r_sub: i8 = a - b; io.out("{}", r_sub);
        let r_mul: i8 = a * b; io.out("{}", r_mul);
        let r_and: i8 = a & b; io.out("{}", r_and);
        let r_or: i8 = a | b; io.out("{}", r_or);
        let r_xor: i8 = a ^ b; io.out("{}", r_xor);
        let r_div: i8 = a / b; io.out("{}", r_div);
        let r_rem: i8 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: i8 = a << 1; io.out("{}", r_shl1);
        let r_shr1: i8 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: i8 = a << 3; io.out("{}", r_shl3);
        let r_shr3: i8 = a >> 3; io.out("{}", r_shr3);
        let r_shl7: i8 = a << 7; io.out("{}", r_shl7);
        let r_shr7: i8 = a >> 7; io.out("{}", r_shr7);
        let r_not: i8 = ~a; io.out("{}", r_not);
        let r_neg: i8 = 0 - a; io.out("{}", r_neg);
    }
    return 0;
}
