// GENERATED by scripts/gen_ops_diff.py: every operator on f32
// The C file next to it is the reference; difftest.sh requires identical output.
import { io } from "std";

func main() -> i8 {
    {
        let a: f32 = 1.0;
        let b: f32 = 3.0;
        let r_add: f32 = a + b; io.out("{}", r_add);
        let r_sub: f32 = a - b; io.out("{}", r_sub);
        let r_mul: f32 = a * b; io.out("{}", r_mul);
        let r_div: f32 = a / b; io.out("{}", r_div);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_neg: f32 = 0.0 - a; io.out("{}", r_neg);
        let r_i: i64 = a as i64; io.out("{}", r_i);
        let r_back: f32 = (a as i64) as f32; io.out("{}", r_back);
    }
    {
        let a: f32 = 2.5;
        let b: f32 = 0.5;
        let r_add: f32 = a + b; io.out("{}", r_add);
        let r_sub: f32 = a - b; io.out("{}", r_sub);
        let r_mul: f32 = a * b; io.out("{}", r_mul);
        let r_div: f32 = a / b; io.out("{}", r_div);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_neg: f32 = 0.0 - a; io.out("{}", r_neg);
        let r_i: i64 = a as i64; io.out("{}", r_i);
        let r_back: f32 = (a as i64) as f32; io.out("{}", r_back);
    }
    {
        let a: f32 = -7.25;
        let b: f32 = 2.0;
        let r_add: f32 = a + b; io.out("{}", r_add);
        let r_sub: f32 = a - b; io.out("{}", r_sub);
        let r_mul: f32 = a * b; io.out("{}", r_mul);
        let r_div: f32 = a / b; io.out("{}", r_div);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_neg: f32 = 0.0 - a; io.out("{}", r_neg);
        let r_i: i64 = a as i64; io.out("{}", r_i);
        let r_back: f32 = (a as i64) as f32; io.out("{}", r_back);
    }
    {
        let a: f32 = 0.0;
        let b: f32 = 5.0;
        let r_add: f32 = a + b; io.out("{}", r_add);
        let r_sub: f32 = a - b; io.out("{}", r_sub);
        let r_mul: f32 = a * b; io.out("{}", r_mul);
        let r_div: f32 = a / b; io.out("{}", r_div);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_neg: f32 = 0.0 - a; io.out("{}", r_neg);
        let r_i: i64 = a as i64; io.out("{}", r_i);
        let r_back: f32 = (a as i64) as f32; io.out("{}", r_back);
    }
    {
        let a: f32 = 1e+18;
        let b: f32 = 3.0;
        let r_add: f32 = a + b; io.out("{}", r_add);
        let r_sub: f32 = a - b; io.out("{}", r_sub);
        let r_mul: f32 = a * b; io.out("{}", r_mul);
        let r_div: f32 = a / b; io.out("{}", r_div);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_neg: f32 = 0.0 - a; io.out("{}", r_neg);
        let r_i: i64 = a as i64; io.out("{}", r_i);
        let r_back: f32 = (a as i64) as f32; io.out("{}", r_back);
    }
    {
        let a: f32 = 0.1;
        let b: f32 = 0.2;
        let r_add: f32 = a + b; io.out("{}", r_add);
        let r_sub: f32 = a - b; io.out("{}", r_sub);
        let r_mul: f32 = a * b; io.out("{}", r_mul);
        let r_div: f32 = a / b; io.out("{}", r_div);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_neg: f32 = 0.0 - a; io.out("{}", r_neg);
        let r_i: i64 = a as i64; io.out("{}", r_i);
        let r_back: f32 = (a as i64) as f32; io.out("{}", r_back);
    }
    {
        let a: f32 = -0.5;
        let b: f32 = -0.25;
        let r_add: f32 = a + b; io.out("{}", r_add);
        let r_sub: f32 = a - b; io.out("{}", r_sub);
        let r_mul: f32 = a * b; io.out("{}", r_mul);
        let r_div: f32 = a / b; io.out("{}", r_div);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_neg: f32 = 0.0 - a; io.out("{}", r_neg);
        let r_i: i64 = a as i64; io.out("{}", r_i);
        let r_back: f32 = (a as i64) as f32; io.out("{}", r_back);
    }
    {
        let a: f32 = 123456.789;
        let b: f32 = 1000.0;
        let r_add: f32 = a + b; io.out("{}", r_add);
        let r_sub: f32 = a - b; io.out("{}", r_sub);
        let r_mul: f32 = a * b; io.out("{}", r_mul);
        let r_div: f32 = a / b; io.out("{}", r_div);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_neg: f32 = 0.0 - a; io.out("{}", r_neg);
        let r_i: i64 = a as i64; io.out("{}", r_i);
        let r_back: f32 = (a as i64) as f32; io.out("{}", r_back);
    }
    {
        let a: f32 = 1e-07;
        let b: f32 = 3.0;
        let r_add: f32 = a + b; io.out("{}", r_add);
        let r_sub: f32 = a - b; io.out("{}", r_sub);
        let r_mul: f32 = a * b; io.out("{}", r_mul);
        let r_div: f32 = a / b; io.out("{}", r_div);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_neg: f32 = 0.0 - a; io.out("{}", r_neg);
        let r_i: i64 = a as i64; io.out("{}", r_i);
        let r_back: f32 = (a as i64) as f32; io.out("{}", r_back);
    }
    return 0;
}
