// GENERATED by scripts/gen_ops_diff.py: every operator on i32
// The C file next to it is the reference; difftest.sh requires identical output.
import { io } from "std";

func main() -> i8 {
    {
        let a: i32 = 2147483647;
        let b: i32 = 1;
        let r_add: i32 = a + b; io.out("{}", r_add);
        let r_sub: i32 = a - b; io.out("{}", r_sub);
        let r_mul: i32 = a * b; io.out("{}", r_mul);
        let r_and: i32 = a & b; io.out("{}", r_and);
        let r_or: i32 = a | b; io.out("{}", r_or);
        let r_xor: i32 = a ^ b; io.out("{}", r_xor);
        let r_div: i32 = a / b; io.out("{}", r_div);
        let r_rem: i32 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: i32 = a << 1; io.out("{}", r_shl1);
        let r_shr1: i32 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: i32 = a << 3; io.out("{}", r_shl3);
        let r_shr3: i32 = a >> 3; io.out("{}", r_shr3);
        let r_shl31: i32 = a << 31; io.out("{}", r_shl31);
        let r_shr31: i32 = a >> 31; io.out("{}", r_shr31);
        let r_not: i32 = ~a; io.out("{}", r_not);
        let r_neg: i32 = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: i32 = -2147483648;
        let b: i32 = 1;
        let r_add: i32 = a + b; io.out("{}", r_add);
        let r_sub: i32 = a - b; io.out("{}", r_sub);
        let r_mul: i32 = a * b; io.out("{}", r_mul);
        let r_and: i32 = a & b; io.out("{}", r_and);
        let r_or: i32 = a | b; io.out("{}", r_or);
        let r_xor: i32 = a ^ b; io.out("{}", r_xor);
        let r_div: i32 = a / b; io.out("{}", r_div);
        let r_rem: i32 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_not: i32 = ~a; io.out("{}", r_not);
        let r_neg: i32 = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: i32 = 2147483647;
        let b: i32 = 3;
        let r_add: i32 = a + b; io.out("{}", r_add);
        let r_sub: i32 = a - b; io.out("{}", r_sub);
        let r_mul: i32 = a * b; io.out("{}", r_mul);
        let r_and: i32 = a & b; io.out("{}", r_and);
        let r_or: i32 = a | b; io.out("{}", r_or);
        let r_xor: i32 = a ^ b; io.out("{}", r_xor);
        let r_div: i32 = a / b; io.out("{}", r_div);
        let r_rem: i32 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: i32 = a << 1; io.out("{}", r_shl1);
        let r_shr1: i32 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: i32 = a << 3; io.out("{}", r_shl3);
        let r_shr3: i32 = a >> 3; io.out("{}", r_shr3);
        let r_shl31: i32 = a << 31; io.out("{}", r_shl31);
        let r_shr31: i32 = a >> 31; io.out("{}", r_shr31);
        let r_not: i32 = ~a; io.out("{}", r_not);
        let r_neg: i32 = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: i32 = -2147483648;
        let b: i32 = 7;
        let r_add: i32 = a + b; io.out("{}", r_add);
        let r_sub: i32 = a - b; io.out("{}", r_sub);
        let r_mul: i32 = a * b; io.out("{}", r_mul);
        let r_and: i32 = a & b; io.out("{}", r_and);
        let r_or: i32 = a | b; io.out("{}", r_or);
        let r_xor: i32 = a ^ b; io.out("{}", r_xor);
        let r_div: i32 = a / b; io.out("{}", r_div);
        let r_rem: i32 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_not: i32 = ~a; io.out("{}", r_not);
        let r_neg: i32 = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: i32 = 0;
        let b: i32 = 5;
        let r_add: i32 = a + b; io.out("{}", r_add);
        let r_sub: i32 = a - b; io.out("{}", r_sub);
        let r_mul: i32 = a * b; io.out("{}", r_mul);
        let r_and: i32 = a & b; io.out("{}", r_and);
        let r_or: i32 = a | b; io.out("{}", r_or);
        let r_xor: i32 = a ^ b; io.out("{}", r_xor);
        let r_div: i32 = a / b; io.out("{}", r_div);
        let r_rem: i32 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: i32 = a << 1; io.out("{}", r_shl1);
        let r_shr1: i32 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: i32 = a << 3; io.out("{}", r_shl3);
        let r_shr3: i32 = a >> 3; io.out("{}", r_shr3);
        let r_shl31: i32 = a << 31; io.out("{}", r_shl31);
        let r_shr31: i32 = a >> 31; io.out("{}", r_shr31);
        let r_not: i32 = ~a; io.out("{}", r_not);
        let r_neg: i32 = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: i32 = -1;
        let b: i32 = -1;
        let r_add: i32 = a + b; io.out("{}", r_add);
        let r_sub: i32 = a - b; io.out("{}", r_sub);
        let r_mul: i32 = a * b; io.out("{}", r_mul);
        let r_and: i32 = a & b; io.out("{}", r_and);
        let r_or: i32 = a | b; io.out("{}", r_or);
        let r_xor: i32 = a ^ b; io.out("{}", r_xor);
        let r_div: i32 = a / b; io.out("{}", r_div);
        let r_rem: i32 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_not: i32 = ~a; io.out("{}", r_not);
        let r_neg: i32 = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: i32 = 7;
        let b: i32 = 3;
        let r_add: i32 = a + b; io.out("{}", r_add);
        let r_sub: i32 = a - b; io.out("{}", r_sub);
        let r_mul: i32 = a * b; io.out("{}", r_mul);
        let r_and: i32 = a & b; io.out("{}", r_and);
        let r_or: i32 = a | b; io.out("{}", r_or);
        let r_xor: i32 = a ^ b; io.out("{}", r_xor);
        let r_div: i32 = a / b; io.out("{}", r_div);
        let r_rem: i32 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: i32 = a << 1; io.out("{}", r_shl1);
        let r_shr1: i32 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: i32 = a << 3; io.out("{}", r_shl3);
        let r_shr3: i32 = a >> 3; io.out("{}", r_shr3);
        let r_shl31: i32 = a << 31; io.out("{}", r_shl31);
        let r_shr31: i32 = a >> 31; io.out("{}", r_shr31);
        let r_not: i32 = ~a; io.out("{}", r_not);
        let r_neg: i32 = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: i32 = -7;
        let b: i32 = 3;
        let r_add: i32 = a + b; io.out("{}", r_add);
        let r_sub: i32 = a - b; io.out("{}", r_sub);
        let r_mul: i32 = a * b; io.out("{}", r_mul);
        let r_and: i32 = a & b; io.out("{}", r_and);
        let r_or: i32 = a | b; io.out("{}", r_or);
        let r_xor: i32 = a ^ b; io.out("{}", r_xor);
        let r_div: i32 = a / b; io.out("{}", r_div);
        let r_rem: i32 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_not: i32 = ~a; io.out("{}", r_not);
        let r_neg: i32 = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: i32 = 7;
        let b: i32 = -3;
        let r_add: i32 = a + b; io.out("{}", r_add);
        let r_sub: i32 = a - b; io.out("{}", r_sub);
        let r_mul: i32 = a * b; io.out("{}", r_mul);
        let r_and: i32 = a & b; io.out("{}", r_and);
        let r_or: i32 = a | b; io.out("{}", r_or);
        let r_xor: i32 = a ^ b; io.out("{}", r_xor);
        let r_div: i32 = a / b; io.out("{}", r_div);
        let r_rem: i32 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: i32 = a << 1; io.out("{}", r_shl1);
        let r_shr1: i32 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: i32 = a << 3; io.out("{}", r_shl3);
        let r_shr3: i32 = a >> 3; io.out("{}", r_shr3);
        let r_shl31: i32 = a << 31; io.out("{}", r_shl31);
        let r_shr31: i32 = a >> 31; io.out("{}", r_shr31);
        let r_not: i32 = ~a; io.out("{}", r_not);
        let r_neg: i32 = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: i32 = -7;
        let b: i32 = -3;
        let r_add: i32 = a + b; io.out("{}", r_add);
        let r_sub: i32 = a - b; io.out("{}", r_sub);
        let r_mul: i32 = a * b; io.out("{}", r_mul);
        let r_and: i32 = a & b; io.out("{}", r_and);
        let r_or: i32 = a | b; io.out("{}", r_or);
        let r_xor: i32 = a ^ b; io.out("{}", r_xor);
        let r_div: i32 = a / b; io.out("{}", r_div);
        let r_rem: i32 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_not: i32 = ~a; io.out("{}", r_not);
        let r_neg: i32 = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: i32 = 1073741826;
        let b: i32 = 9;
        let r_add: i32 = a + b; io.out("{}", r_add);
        let r_sub: i32 = a - b; io.out("{}", r_sub);
        let r_mul: i32 = a * b; io.out("{}", r_mul);
        let r_and: i32 = a & b; io.out("{}", r_and);
        let r_or: i32 = a | b; io.out("{}", r_or);
        let r_xor: i32 = a ^ b; io.out("{}", r_xor);
        let r_div: i32 = a / b; io.out("{}", r_div);
        let r_rem: i32 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: i32 = a << 1; io.out("{}", r_shl1);
        let r_shr1: i32 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: i32 = a << 3; io.out("{}", r_shl3);
        let r_shr3: i32 = a >> 3; io.out("{}", r_shr3);
        let r_shl31: i32 = a << 31; io.out("{}", r_shl31);
        let r_shr31: i32 = a >> 31; io.out("{}", r_shr31);
        let r_not: i32 = ~a; io.out("{}", r_not);
        let r_neg: i32 = 0 - a; io.out("{}", r_neg);
    }
    return 0;
}
