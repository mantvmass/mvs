// MVS Language

var welcome: string = "ยินดีตอนรับ";

struct User {
    email: string,
    active: bool,
    username: string,
}

func main() -> int {

    struct Monster {
        name: string,
        damage: int,
        something: Array<string>,
    }

    var monster: Monster = {
        name: "mvs",
        damage: 19,
        something: ["dog", "cat"],
    };
    
    var user: User = {
        email: "mantvmass@notmail.com",
        active: true,
        username: "mvs",
    };

    var a: string;
    a = "mvs";
    
    var b: string = "mvs";

    var c: int;
    c = 0;

    var d: int = 0;

    var e: float;
    e = 3.14;
    
    var f: float = 3.14;

    var g: bool;
    g = true;

    var h: bool = true;

    var i: Array<int>;
    var j: Array<string>;
    i = [1,5,6,8];
    j = ["Hello", "สวัสดีครับ"];

    var k: Array<int> = [1, 2, 3];
    var l: Array<string> = ["dog", "cat", ""];
    
    return c;
}