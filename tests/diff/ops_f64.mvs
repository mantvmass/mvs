// GENERATED by scripts/gen_ops_diff.py: every operator on f64
// The C file next to it is the reference; difftest.sh requires identical output.
import { io } from "std";

func main() -> i8 {
    {
        let a: f64 = 1.0;
        let b: f64 = 3.0;
        let r_add: f64 = a + b; io.out("{}", r_add);
        let r_sub: f64 = a - b; io.out("{}", r_sub);
        let r_mul: f64 = a * b; io.out("{}", r_mul);
        let r_div: f64 = a / b; io.out("{}", r_div);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_neg: f64 = 0.0 - a; io.out("{}", r_neg);
        let r_i: i64 = a as i64; io.out("{}", r_i);
        let r_back: f64 = (a as i64) as f64; io.out("{}", r_back);
    }
    {
        let a: f64 = 2.5;
        let b: f64 = 0.5;
        let r_add: f64 = a + b; io.out("{}", r_add);
        let r_sub: f64 = a - b; io.out("{}", r_sub);
        let r_mul: f64 = a * b; io.out("{}", r_mul);
        let r_div: f64 = a / b; io.out("{}", r_div);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_neg: f64 = 0.0 - a; io.out("{}", r_neg);
        let r_i: i64 = a as i64; io.out("{}", r_i);
        let r_back: f64 = (a as i64) as f64; io.out("{}", r_back);
    }
    {
        let a: f64 = -7.25;
        let b: f64 = 2.0;
        let r_add: f64 = a + b; io.out("{}", r_add);
        let r_sub: f64 = a - b; io.out("{}", r_sub);
        let r_mul: f64 = a * b; io.out("{}", r_mul);
        let r_div: f64 = a / b; io.out("{}", r_div);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_neg: f64 = 0.0 - a; io.out("{}", r_neg);
        let r_i: i64 = a as i64; io.out("{}", r_i);
        let r_back: f64 = (a as i64) as f64; io.out("{}", r_back);
    }
    {
        let a: f64 = 0.0;
        let b: f64 = 5.0;
        let r_add: f64 = a + b; io.out("{}", r_add);
        let r_sub: f64 = a - b; io.out("{}", r_sub);
        let r_mul: f64 = a * b; io.out("{}", r_mul);
        let r_div: f64 = a / b; io.out("{}", r_div);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_neg: f64 = 0.0 - a; io.out("{}", r_neg);
        let r_i: i64 = a as i64; io.out("{}", r_i);
        let r_back: f64 = (a as i64) as f64; io.out("{}", r_back);
    }
    {
        let a: f64 = 1e+18;
        let b: f64 = 3.0;
        let r_add: f64 = a + b; io.out("{}", r_add);
        let r_sub: f64 = a - b; io.out("{}", r_sub);
        let r_mul: f64 = a * b; io.out("{}", r_mul);
        let r_div: f64 = a / b; io.out("{}", r_div);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_neg: f64 = 0.0 - a; io.out("{}", r_neg);
        let r_i: i64 = a as i64; io.out("{}", r_i);
        let r_back: f64 = (a as i64) as f64; io.out("{}", r_back);
    }
    {
        let a: f64 = 0.1;
        let b: f64 = 0.2;
        let r_add: f64 = a + b; io.out("{}", r_add);
        let r_sub: f64 = a - b; io.out("{}", r_sub);
        let r_mul: f64 = a * b; io.out("{}", r_mul);
        let r_div: f64 = a / b; io.out("{}", r_div);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_neg: f64 = 0.0 - a; io.out("{}", r_neg);
        let r_i: i64 = a as i64; io.out("{}", r_i);
        let r_back: f64 = (a as i64) as f64; io.out("{}", r_back);
    }
    {
        let a: f64 = -0.5;
        let b: f64 = -0.25;
        let r_add: f64 = a + b; io.out("{}", r_add);
        let r_sub: f64 = a - b; io.out("{}", r_sub);
        let r_mul: f64 = a * b; io.out("{}", r_mul);
        let r_div: f64 = a / b; io.out("{}", r_div);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_neg: f64 = 0.0 - a; io.out("{}", r_neg);
        let r_i: i64 = a as i64; io.out("{}", r_i);
        let r_back: f64 = (a as i64) as f64; io.out("{}", r_back);
    }
    {
        let a: f64 = 123456.789;
        let b: f64 = 1000.0;
        let r_add: f64 = a + b; io.out("{}", r_add);
        let r_sub: f64 = a - b; io.out("{}", r_sub);
        let r_mul: f64 = a * b; io.out("{}", r_mul);
        let r_div: f64 = a / b; io.out("{}", r_div);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_neg: f64 = 0.0 - a; io.out("{}", r_neg);
        let r_i: i64 = a as i64; io.out("{}", r_i);
        let r_back: f64 = (a as i64) as f64; io.out("{}", r_back);
    }
    {
        let a: f64 = 1e-07;
        let b: f64 = 3.0;
        let r_add: f64 = a + b; io.out("{}", r_add);
        let r_sub: f64 = a - b; io.out("{}", r_sub);
        let r_mul: f64 = a * b; io.out("{}", r_mul);
        let r_div: f64 = a / b; io.out("{}", r_div);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_neg: f64 = 0.0 - a; io.out("{}", r_neg);
        let r_i: i64 = a as i64; io.out("{}", r_i);
        let r_back: f64 = (a as i64) as f64; io.out("{}", r_back);
    }
    return 0;
}
