// GENERATED by scripts/gen_ops_diff.py: every operator on i64
// The C file next to it is the reference; difftest.sh requires identical output.
import { io } from "std";

func main() -> i8 {
    {
        let a: i64 = 9223372036854775807;
        let b: i64 = 1;
        let r_add: i64 = a + b; io.out("{}", r_add);
        let r_sub: i64 = a - b; io.out("{}", r_sub);
        let r_mul: i64 = a * b; io.out("{}", r_mul);
        let r_and: i64 = a & b; io.out("{}", r_and);
        let r_or: i64 = a | b; io.out("{}", r_or);
        let r_xor: i64 = a ^ b; io.out("{}", r_xor);
        let r_div: i64 = a / b; io.out("{}", r_div);
        let r_rem: i64 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: i64 = a << 1; io.out("{}", r_shl1);
        let r_shr1: i64 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: i64 = a << 3; io.out("{}", r_shl3);
        let r_shr3: i64 = a >> 3; io.out("{}", r_shr3);
        let r_shl63: i64 = a << 63; io.out("{}", r_shl63);
        let r_shr63: i64 = a >> 63; io.out("{}", r_shr63);
        let r_not: i64 = ~a; io.out("{}", r_not);
        let r_neg: i64 = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: i64 = -9223372036854775808;
        let b: i64 = 1;
        let r_add: i64 = a + b; io.out("{}", r_add);
        let r_sub: i64 = a - b; io.out("{}", r_sub);
        let r_mul: i64 = a * b; io.out("{}", r_mul);
        let r_and: i64 = a & b; io.out("{}", r_and);
        let r_or: i64 = a | b; io.out("{}", r_or);
        let r_xor: i64 = a ^ b; io.out("{}", r_xor);
        let r_div: i64 = a / b; io.out("{}", r_div);
        let r_rem: i64 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_not: i64 = ~a; io.out("{}", r_not);
        let r_neg: i64 = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: i64 = 9223372036854775807;
        let b: i64 = 3;
        let r_add: i64 = a + b; io.out("{}", r_add);
        let r_sub: i64 = a - b; io.out("{}", r_sub);
        let r_mul: i64 = a * b; io.out("{}", r_mul);
        let r_and: i64 = a & b; io.out("{}", r_and);
        let r_or: i64 = a | b; io.out("{}", r_or);
        let r_xor: i64 = a ^ b; io.out("{}", r_xor);
        let r_div: i64 = a / b; io.out("{}", r_div);
        let r_rem: i64 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: i64 = a << 1; io.out("{}", r_shl1);
        let r_shr1: i64 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: i64 = a << 3; io.out("{}", r_shl3);
        let r_shr3: i64 = a >> 3; io.out("{}", r_shr3);
        let r_shl63: i64 = a << 63; io.out("{}", r_shl63);
        let r_shr63: i64 = a >> 63; io.out("{}", r_shr63);
        let r_not: i64 = ~a; io.out("{}", r_not);
        let r_neg: i64 = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: i64 = -9223372036854775808;
        let b: i64 = 7;
        let r_add: i64 = a + b; io.out("{}", r_add);
        let r_sub: i64 = a - b; io.out("{}", r_sub);
        let r_mul: i64 = a * b; io.out("{}", r_mul);
        let r_and: i64 = a & b; io.out("{}", r_and);
        let r_or: i64 = a | b; io.out("{}", r_or);
        let r_xor: i64 = a ^ b; io.out("{}", r_xor);
        let r_div: i64 = a / b; io.out("{}", r_div);
        let r_rem: i64 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_not: i64 = ~a; io.out("{}", r_not);
        let r_neg: i64 = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: i64 = 0;
        let b: i64 = 5;
        let r_add: i64 = a + b; io.out("{}", r_add);
        let r_sub: i64 = a - b; io.out("{}", r_sub);
        let r_mul: i64 = a * b; io.out("{}", r_mul);
        let r_and: i64 = a & b; io.out("{}", r_and);
        let r_or: i64 = a | b; io.out("{}", r_or);
        let r_xor: i64 = a ^ b; io.out("{}", r_xor);
        let r_div: i64 = a / b; io.out("{}", r_div);
        let r_rem: i64 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: i64 = a << 1; io.out("{}", r_shl1);
        let r_shr1: i64 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: i64 = a << 3; io.out("{}", r_shl3);
        let r_shr3: i64 = a >> 3; io.out("{}", r_shr3);
        let r_shl63: i64 = a << 63; io.out("{}", r_shl63);
        let r_shr63: i64 = a >> 63; io.out("{}", r_shr63);
        let r_not: i64 = ~a; io.out("{}", r_not);
        let r_neg: i64 = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: i64 = -1;
        let b: i64 = -1;
        let r_add: i64 = a + b; io.out("{}", r_add);
        let r_sub: i64 = a - b; io.out("{}", r_sub);
        let r_mul: i64 = a * b; io.out("{}", r_mul);
        let r_and: i64 = a & b; io.out("{}", r_and);
        let r_or: i64 = a | b; io.out("{}", r_or);
        let r_xor: i64 = a ^ b; io.out("{}", r_xor);
        let r_div: i64 = a / b; io.out("{}", r_div);
        let r_rem: i64 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_not: i64 = ~a; io.out("{}", r_not);
        let r_neg: i64 = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: i64 = 7;
        let b: i64 = 3;
        let r_add: i64 = a + b; io.out("{}", r_add);
        let r_sub: i64 = a - b; io.out("{}", r_sub);
        let r_mul: i64 = a * b; io.out("{}", r_mul);
        let r_and: i64 = a & b; io.out("{}", r_and);
        let r_or: i64 = a | b; io.out("{}", r_or);
        let r_xor: i64 = a ^ b; io.out("{}", r_xor);
        let r_div: i64 = a / b; io.out("{}", r_div);
        let r_rem: i64 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: i64 = a << 1; io.out("{}", r_shl1);
        let r_shr1: i64 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: i64 = a << 3; io.out("{}", r_shl3);
        let r_shr3: i64 = a >> 3; io.out("{}", r_shr3);
        let r_shl63: i64 = a << 63; io.out("{}", r_shl63);
        let r_shr63: i64 = a >> 63; io.out("{}", r_shr63);
        let r_not: i64 = ~a; io.out("{}", r_not);
        let r_neg: i64 = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: i64 = -7;
        let b: i64 = 3;
        let r_add: i64 = a + b; io.out("{}", r_add);
        let r_sub: i64 = a - b; io.out("{}", r_sub);
        let r_mul: i64 = a * b; io.out("{}", r_mul);
        let r_and: i64 = a & b; io.out("{}", r_and);
        let r_or: i64 = a | b; io.out("{}", r_or);
        let r_xor: i64 = a ^ b; io.out("{}", r_xor);
        let r_div: i64 = a / b; io.out("{}", r_div);
        let r_rem: i64 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_not: i64 = ~a; io.out("{}", r_not);
        let r_neg: i64 = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: i64 = 7;
        let b: i64 = -3;
        let r_add: i64 = a + b; io.out("{}", r_add);
        let r_sub: i64 = a - b; io.out("{}", r_sub);
        let r_mul: i64 = a * b; io.out("{}", r_mul);
        let r_and: i64 = a & b; io.out("{}", r_and);
        let r_or: i64 = a | b; io.out("{}", r_or);
        let r_xor: i64 = a ^ b; io.out("{}", r_xor);
        let r_div: i64 = a / b; io.out("{}", r_div);
        let r_rem: i64 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: i64 = a << 1; io.out("{}", r_shl1);
        let r_shr1: i64 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: i64 = a << 3; io.out("{}", r_shl3);
        let r_shr3: i64 = a >> 3; io.out("{}", r_shr3);
        let r_shl63: i64 = a << 63; io.out("{}", r_shl63);
        let r_shr63: i64 = a >> 63; io.out("{}", r_shr63);
        let r_not: i64 = ~a; io.out("{}", r_not);
        let r_neg: i64 = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: i64 = -7;
        let b: i64 = -3;
        let r_add: i64 = a + b; io.out("{}", r_add);
        let r_sub: i64 = a - b; io.out("{}", r_sub);
        let r_mul: i64 = a * b; io.out("{}", r_mul);
        let r_and: i64 = a & b; io.out("{}", r_and);
        let r_or: i64 = a | b; io.out("{}", r_or);
        let r_xor: i64 = a ^ b; io.out("{}", r_xor);
        let r_div: i64 = a / b; io.out("{}", r_div);
        let r_rem: i64 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_not: i64 = ~a; io.out("{}", r_not);
        let r_neg: i64 = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: i64 = 4611686018427387906;
        let b: i64 = 9;
        let r_add: i64 = a + b; io.out("{}", r_add);
        let r_sub: i64 = a - b; io.out("{}", r_sub);
        let r_mul: i64 = a * b; io.out("{}", r_mul);
        let r_and: i64 = a & b; io.out("{}", r_and);
        let r_or: i64 = a | b; io.out("{}", r_or);
        let r_xor: i64 = a ^ b; io.out("{}", r_xor);
        let r_div: i64 = a / b; io.out("{}", r_div);
        let r_rem: i64 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: i64 = a << 1; io.out("{}", r_shl1);
        let r_shr1: i64 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: i64 = a << 3; io.out("{}", r_shl3);
        let r_shr3: i64 = a >> 3; io.out("{}", r_shr3);
        let r_shl63: i64 = a << 63; io.out("{}", r_shl63);
        let r_shr63: i64 = a >> 63; io.out("{}", r_shr63);
        let r_not: i64 = ~a; io.out("{}", r_not);
        let r_neg: i64 = 0 - a; io.out("{}", r_neg);
    }
    return 0;
}
