// ============================================================
//  mathlib — โมดูลที่ผู้ใช้เขียนเอง (นำเข้าโดย use_import.mvs)
//  ไม่ต้อง build เดี่ยว — ถูก import โดยไฟล์อื่น
// ============================================================

func square(x: i32) -> i32 { return x * x; }

func factorial(n: i32) -> i32 {
    let r: i32 = 1;
    for (let i: i32 = 1; i <= n; i++) { r = r * i; }
    return r;
}
