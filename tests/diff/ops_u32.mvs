// GENERATED by scripts/gen_ops_diff.py: every operator on u32
// The C file next to it is the reference; difftest.sh requires identical output.
import { io } from "std";

func main() -> i8 {
    {
        let a: u32 = 4294967295;
        let b: u32 = 1;
        let r_add: u32 = a + b; io.out("{}", r_add);
        let r_sub: u32 = a - b; io.out("{}", r_sub);
        let r_mul: u32 = a * b; io.out("{}", r_mul);
        let r_and: u32 = a & b; io.out("{}", r_and);
        let r_or: u32 = a | b; io.out("{}", r_or);
        let r_xor: u32 = a ^ b; io.out("{}", r_xor);
        let r_div: u32 = a / b; io.out("{}", r_div);
        let r_rem: u32 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: u32 = a << 1; io.out("{}", r_shl1);
        let r_shr1: u32 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: u32 = a << 3; io.out("{}", r_shl3);
        let r_shr3: u32 = a >> 3; io.out("{}", r_shr3);
        let r_shl31: u32 = a << 31; io.out("{}", r_shl31);
        let r_shr31: u32 = a >> 31; io.out("{}", r_shr31);
        let r_not: u32 = ~a; io.out("{}", r_not);
    }
    {
        let a: u32 = 0;
        let b: u32 = 5;
        let r_add: u32 = a + b; io.out("{}", r_add);
        let r_sub: u32 = a - b; io.out("{}", r_sub);
        let r_mul: u32 = a * b; io.out("{}", r_mul);
        let r_and: u32 = a & b; io.out("{}", r_and);
        let r_or: u32 = a | b; io.out("{}", r_or);
        let r_xor: u32 = a ^ b; io.out("{}", r_xor);
        let r_div: u32 = a / b; io.out("{}", r_div);
        let r_rem: u32 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: u32 = a << 1; io.out("{}", r_shl1);
        let r_shr1: u32 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: u32 = a << 3; io.out("{}", r_shl3);
        let r_shr3: u32 = a >> 3; io.out("{}", r_shr3);
        let r_shl31: u32 = a << 31; io.out("{}", r_shl31);
        let r_shr31: u32 = a >> 31; io.out("{}", r_shr31);
        let r_not: u32 = ~a; io.out("{}", r_not);
    }
    {
        let a: u32 = 4294967295;
        let b: u32 = 3;
        let r_add: u32 = a + b; io.out("{}", r_add);
        let r_sub: u32 = a - b; io.out("{}", r_sub);
        let r_mul: u32 = a * b; io.out("{}", r_mul);
        let r_and: u32 = a & b; io.out("{}", r_and);
        let r_or: u32 = a | b; io.out("{}", r_or);
        let r_xor: u32 = a ^ b; io.out("{}", r_xor);
        let r_div: u32 = a / b; io.out("{}", r_div);
        let r_rem: u32 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: u32 = a << 1; io.out("{}", r_shl1);
        let r_shr1: u32 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: u32 = a << 3; io.out("{}", r_shl3);
        let r_shr3: u32 = a >> 3; io.out("{}", r_shr3);
        let r_shl31: u32 = a << 31; io.out("{}", r_shl31);
        let r_shr31: u32 = a >> 31; io.out("{}", r_shr31);
        let r_not: u32 = ~a; io.out("{}", r_not);
    }
    {
        let a: u32 = 2147483650;
        let b: u32 = 9;
        let r_add: u32 = a + b; io.out("{}", r_add);
        let r_sub: u32 = a - b; io.out("{}", r_sub);
        let r_mul: u32 = a * b; io.out("{}", r_mul);
        let r_and: u32 = a & b; io.out("{}", r_and);
        let r_or: u32 = a | b; io.out("{}", r_or);
        let r_xor: u32 = a ^ b; io.out("{}", r_xor);
        let r_div: u32 = a / b; io.out("{}", r_div);
        let r_rem: u32 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: u32 = a << 1; io.out("{}", r_shl1);
        let r_shr1: u32 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: u32 = a << 3; io.out("{}", r_shl3);
        let r_shr3: u32 = a >> 3; io.out("{}", r_shr3);
        let r_shl31: u32 = a << 31; io.out("{}", r_shl31);
        let r_shr31: u32 = a >> 31; io.out("{}", r_shr31);
        let r_not: u32 = ~a; io.out("{}", r_not);
    }
    {
        let a: u32 = 7;
        let b: u32 = 3;
        let r_add: u32 = a + b; io.out("{}", r_add);
        let r_sub: u32 = a - b; io.out("{}", r_sub);
        let r_mul: u32 = a * b; io.out("{}", r_mul);
        let r_and: u32 = a & b; io.out("{}", r_and);
        let r_or: u32 = a | b; io.out("{}", r_or);
        let r_xor: u32 = a ^ b; io.out("{}", r_xor);
        let r_div: u32 = a / b; io.out("{}", r_div);
        let r_rem: u32 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: u32 = a << 1; io.out("{}", r_shl1);
        let r_shr1: u32 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: u32 = a << 3; io.out("{}", r_shl3);
        let r_shr3: u32 = a >> 3; io.out("{}", r_shr3);
        let r_shl31: u32 = a << 31; io.out("{}", r_shl31);
        let r_shr31: u32 = a >> 31; io.out("{}", r_shr31);
        let r_not: u32 = ~a; io.out("{}", r_not);
    }
    {
        let a: u32 = 200;
        let b: u32 = 7;
        let r_add: u32 = a + b; io.out("{}", r_add);
        let r_sub: u32 = a - b; io.out("{}", r_sub);
        let r_mul: u32 = a * b; io.out("{}", r_mul);
        let r_and: u32 = a & b; io.out("{}", r_and);
        let r_or: u32 = a | b; io.out("{}", r_or);
        let r_xor: u32 = a ^ b; io.out("{}", r_xor);
        let r_div: u32 = a / b; io.out("{}", r_div);
        let r_rem: u32 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: u32 = a << 1; io.out("{}", r_shl1);
        let r_shr1: u32 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: u32 = a << 3; io.out("{}", r_shl3);
        let r_shr3: u32 = a >> 3; io.out("{}", r_shr3);
        let r_shl31: u32 = a << 31; io.out("{}", r_shl31);
        let r_shr31: u32 = a >> 31; io.out("{}", r_shr31);
        let r_not: u32 = ~a; io.out("{}", r_not);
    }
    return 0;
}
