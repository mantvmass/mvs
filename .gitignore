# the built compiler
mvs
mvs.exe

# intermediate/output files from compilation (every folder)
# (*.s covers the arm64 backend's GNU-as output; no assembly sources are checked in)
*.asm
*.obj
*.o
*.s
*.exe
*.ilk
*.pdb
*.lib

# files created by examples at run time
# (greeting.txt comes from examples/08_stdlib/files.mvs)
greeting.txt

# experimental/temporary folders
tmp/
_t/

# editor / IDE / OS
.vscode/
.idea/
*.swp
*~
.DS_Store
Thumbs.db

# fuzzer reproducers (scripts/fuzz.sh writes these next to the repo root)
fuzz-crash-*.mvs
fuzz-san-*.mvs
mvs_asan
