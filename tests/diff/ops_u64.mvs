// GENERATED by scripts/gen_ops_diff.py: every operator on u64
// The C file next to it is the reference; difftest.sh requires identical output.
import { io } from "std";

func main() -> i8 {
    {
        let a: u64 = 18446744073709551615;
        let b: u64 = 1;
        let r_add: u64 = a + b; io.out("{}", r_add);
        let r_sub: u64 = a - b; io.out("{}", r_sub);
        let r_mul: u64 = a * b; io.out("{}", r_mul);
        let r_and: u64 = a & b; io.out("{}", r_and);
        let r_or: u64 = a | b; io.out("{}", r_or);
        let r_xor: u64 = a ^ b; io.out("{}", r_xor);
        let r_div: u64 = a / b; io.out("{}", r_div);
        let r_rem: u64 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: u64 = a << 1; io.out("{}", r_shl1);
        let r_shr1: u64 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: u64 = a << 3; io.out("{}", r_shl3);
        let r_shr3: u64 = a >> 3; io.out("{}", r_shr3);
        let r_shl63: u64 = a << 63; io.out("{}", r_shl63);
        let r_shr63: u64 = a >> 63; io.out("{}", r_shr63);
        let r_not: u64 = ~a; io.out("{}", r_not);
    }
    {
        let a: u64 = 0;
        let b: u64 = 5;
        let r_add: u64 = a + b; io.out("{}", r_add);
        let r_sub: u64 = a - b; io.out("{}", r_sub);
        let r_mul: u64 = a * b; io.out("{}", r_mul);
        let r_and: u64 = a & b; io.out("{}", r_and);
        let r_or: u64 = a | b; io.out("{}", r_or);
        let r_xor: u64 = a ^ b; io.out("{}", r_xor);
        let r_div: u64 = a / b; io.out("{}", r_div);
        let r_rem: u64 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: u64 = a << 1; io.out("{}", r_shl1);
        let r_shr1: u64 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: u64 = a << 3; io.out("{}", r_shl3);
        let r_shr3: u64 = a >> 3; io.out("{}", r_shr3);
        let r_shl63: u64 = a << 63; io.out("{}", r_shl63);
        let r_shr63: u64 = a >> 63; io.out("{}", r_shr63);
        let r_not: u64 = ~a; io.out("{}", r_not);
    }
    {
        let a: u64 = 18446744073709551615;
        let b: u64 = 3;
        let r_add: u64 = a + b; io.out("{}", r_add);
        let r_sub: u64 = a - b; io.out("{}", r_sub);
        let r_mul: u64 = a * b; io.out("{}", r_mul);
        let r_and: u64 = a & b; io.out("{}", r_and);
        let r_or: u64 = a | b; io.out("{}", r_or);
        let r_xor: u64 = a ^ b; io.out("{}", r_xor);
        let r_div: u64 = a / b; io.out("{}", r_div);
        let r_rem: u64 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: u64 = a << 1; io.out("{}", r_shl1);
        let r_shr1: u64 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: u64 = a << 3; io.out("{}", r_shl3);
        let r_shr3: u64 = a >> 3; io.out("{}", r_shr3);
        let r_shl63: u64 = a << 63; io.out("{}", r_shl63);
        let r_shr63: u64 = a >> 63; io.out("{}", r_shr63);
        let r_not: u64 = ~a; io.out("{}", r_not);
    }
    {
        let a: u64 = 9223372036854775810;
        let b: u64 = 9;
        let r_add: u64 = a + b; io.out("{}", r_add);
        let r_sub: u64 = a - b; io.out("{}", r_sub);
        let r_mul: u64 = a * b; io.out("{}", r_mul);
        let r_and: u64 = a & b; io.out("{}", r_and);
        let r_or: u64 = a | b; io.out("{}", r_or);
        let r_xor: u64 = a ^ b; io.out("{}", r_xor);
        let r_div: u64 = a / b; io.out("{}", r_div);
        let r_rem: u64 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: u64 = a << 1; io.out("{}", r_shl1);
        let r_shr1: u64 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: u64 = a << 3; io.out("{}", r_shl3);
        let r_shr3: u64 = a >> 3; io.out("{}", r_shr3);
        let r_shl63: u64 = a << 63; io.out("{}", r_shl63);
        let r_shr63: u64 = a >> 63; io.out("{}", r_shr63);
        let r_not: u64 = ~a; io.out("{}", r_not);
    }
    {
        let a: u64 = 7;
        let b: u64 = 3;
        let r_add: u64 = a + b; io.out("{}", r_add);
        let r_sub: u64 = a - b; io.out("{}", r_sub);
        let r_mul: u64 = a * b; io.out("{}", r_mul);
        let r_and: u64 = a & b; io.out("{}", r_and);
        let r_or: u64 = a | b; io.out("{}", r_or);
        let r_xor: u64 = a ^ b; io.out("{}", r_xor);
        let r_div: u64 = a / b; io.out("{}", r_div);
        let r_rem: u64 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: u64 = a << 1; io.out("{}", r_shl1);
        let r_shr1: u64 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: u64 = a << 3; io.out("{}", r_shl3);
        let r_shr3: u64 = a >> 3; io.out("{}", r_shr3);
        let r_shl63: u64 = a << 63; io.out("{}", r_shl63);
        let r_shr63: u64 = a >> 63; io.out("{}", r_shr63);
        let r_not: u64 = ~a; io.out("{}", r_not);
    }
    {
        let a: u64 = 200;
        let b: u64 = 7;
        let r_add: u64 = a + b; io.out("{}", r_add);
        let r_sub: u64 = a - b; io.out("{}", r_sub);
        let r_mul: u64 = a * b; io.out("{}", r_mul);
        let r_and: u64 = a & b; io.out("{}", r_and);
        let r_or: u64 = a | b; io.out("{}", r_or);
        let r_xor: u64 = a ^ b; io.out("{}", r_xor);
        let r_div: u64 = a / b; io.out("{}", r_div);
        let r_rem: u64 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: u64 = a << 1; io.out("{}", r_shl1);
        let r_shr1: u64 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: u64 = a << 3; io.out("{}", r_shl3);
        let r_shr3: u64 = a >> 3; io.out("{}", r_shr3);
        let r_shl63: u64 = a << 63; io.out("{}", r_shl63);
        let r_shr63: u64 = a >> 63; io.out("{}", r_shr63);
        let r_not: u64 = ~a; io.out("{}", r_not);
    }
    return 0;
}
