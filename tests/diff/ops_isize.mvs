// GENERATED by scripts/gen_ops_diff.py: every operator on isize
// The C file next to it is the reference; difftest.sh requires identical output.
import { io } from "std";

func main() -> i8 {
    {
        let a: isize = 9223372036854775807;
        let b: isize = 1;
        let r_add: isize = a + b; io.out("{}", r_add);
        let r_sub: isize = a - b; io.out("{}", r_sub);
        let r_mul: isize = a * b; io.out("{}", r_mul);
        let r_and: isize = a & b; io.out("{}", r_and);
        let r_or: isize = a | b; io.out("{}", r_or);
        let r_xor: isize = a ^ b; io.out("{}", r_xor);
        let r_div: isize = a / b; io.out("{}", r_div);
        let r_rem: isize = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: isize = a << 1; io.out("{}", r_shl1);
        let r_shr1: isize = a >> 1; io.out("{}", r_shr1);
        let r_shl3: isize = a << 3; io.out("{}", r_shl3);
        let r_shr3: isize = a >> 3; io.out("{}", r_shr3);
        let r_shl63: isize = a << 63; io.out("{}", r_shl63);
        let r_shr63: isize = a >> 63; io.out("{}", r_shr63);
        let r_not: isize = ~a; io.out("{}", r_not);
        let r_neg: isize = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: isize = -9223372036854775808;
        let b: isize = 1;
        let r_add: isize = a + b; io.out("{}", r_add);
        let r_sub: isize = a - b; io.out("{}", r_sub);
        let r_mul: isize = a * b; io.out("{}", r_mul);
        let r_and: isize = a & b; io.out("{}", r_and);
        let r_or: isize = a | b; io.out("{}", r_or);
        let r_xor: isize = a ^ b; io.out("{}", r_xor);
        let r_div: isize = a / b; io.out("{}", r_div);
        let r_rem: isize = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_not: isize = ~a; io.out("{}", r_not);
        let r_neg: isize = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: isize = 9223372036854775807;
        let b: isize = 3;
        let r_add: isize = a + b; io.out("{}", r_add);
        let r_sub: isize = a - b; io.out("{}", r_sub);
        let r_mul: isize = a * b; io.out("{}", r_mul);
        let r_and: isize = a & b; io.out("{}", r_and);
        let r_or: isize = a | b; io.out("{}", r_or);
        let r_xor: isize = a ^ b; io.out("{}", r_xor);
        let r_div: isize = a / b; io.out("{}", r_div);
        let r_rem: isize = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: isize = a << 1; io.out("{}", r_shl1);
        let r_shr1: isize = a >> 1; io.out("{}", r_shr1);
        let r_shl3: isize = a << 3; io.out("{}", r_shl3);
        let r_shr3: isize = a >> 3; io.out("{}", r_shr3);
        let r_shl63: isize = a << 63; io.out("{}", r_shl63);
        let r_shr63: isize = a >> 63; io.out("{}", r_shr63);
        let r_not: isize = ~a; io.out("{}", r_not);
        let r_neg: isize = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: isize = -9223372036854775808;
        let b: isize = 7;
        let r_add: isize = a + b; io.out("{}", r_add);
        let r_sub: isize = a - b; io.out("{}", r_sub);
        let r_mul: isize = a * b; io.out("{}", r_mul);
        let r_and: isize = a & b; io.out("{}", r_and);
        let r_or: isize = a | b; io.out("{}", r_or);
        let r_xor: isize = a ^ b; io.out("{}", r_xor);
        let r_div: isize = a / b; io.out("{}", r_div);
        let r_rem: isize = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_not: isize = ~a; io.out("{}", r_not);
        let r_neg: isize = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: isize = 0;
        let b: isize = 5;
        let r_add: isize = a + b; io.out("{}", r_add);
        let r_sub: isize = a - b; io.out("{}", r_sub);
        let r_mul: isize = a * b; io.out("{}", r_mul);
        let r_and: isize = a & b; io.out("{}", r_and);
        let r_or: isize = a | b; io.out("{}", r_or);
        let r_xor: isize = a ^ b; io.out("{}", r_xor);
        let r_div: isize = a / b; io.out("{}", r_div);
        let r_rem: isize = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: isize = a << 1; io.out("{}", r_shl1);
        let r_shr1: isize = a >> 1; io.out("{}", r_shr1);
        let r_shl3: isize = a << 3; io.out("{}", r_shl3);
        let r_shr3: isize = a >> 3; io.out("{}", r_shr3);
        let r_shl63: isize = a << 63; io.out("{}", r_shl63);
        let r_shr63: isize = a >> 63; io.out("{}", r_shr63);
        let r_not: isize = ~a; io.out("{}", r_not);
        let r_neg: isize = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: isize = -1;
        let b: isize = -1;
        let r_add: isize = a + b; io.out("{}", r_add);
        let r_sub: isize = a - b; io.out("{}", r_sub);
        let r_mul: isize = a * b; io.out("{}", r_mul);
        let r_and: isize = a & b; io.out("{}", r_and);
        let r_or: isize = a | b; io.out("{}", r_or);
        let r_xor: isize = a ^ b; io.out("{}", r_xor);
        let r_div: isize = a / b; io.out("{}", r_div);
        let r_rem: isize = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_not: isize = ~a; io.out("{}", r_not);
        let r_neg: isize = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: isize = 7;
        let b: isize = 3;
        let r_add: isize = a + b; io.out("{}", r_add);
        let r_sub: isize = a - b; io.out("{}", r_sub);
        let r_mul: isize = a * b; io.out("{}", r_mul);
        let r_and: isize = a & b; io.out("{}", r_and);
        let r_or: isize = a | b; io.out("{}", r_or);
        let r_xor: isize = a ^ b; io.out("{}", r_xor);
        let r_div: isize = a / b; io.out("{}", r_div);
        let r_rem: isize = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: isize = a << 1; io.out("{}", r_shl1);
        let r_shr1: isize = a >> 1; io.out("{}", r_shr1);
        let r_shl3: isize = a << 3; io.out("{}", r_shl3);
        let r_shr3: isize = a >> 3; io.out("{}", r_shr3);
        let r_shl63: isize = a << 63; io.out("{}", r_shl63);
        let r_shr63: isize = a >> 63; io.out("{}", r_shr63);
        let r_not: isize = ~a; io.out("{}", r_not);
        let r_neg: isize = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: isize = -7;
        let b: isize = 3;
        let r_add: isize = a + b; io.out("{}", r_add);
        let r_sub: isize = a - b; io.out("{}", r_sub);
        let r_mul: isize = a * b; io.out("{}", r_mul);
        let r_and: isize = a & b; io.out("{}", r_and);
        let r_or: isize = a | b; io.out("{}", r_or);
        let r_xor: isize = a ^ b; io.out("{}", r_xor);
        let r_div: isize = a / b; io.out("{}", r_div);
        let r_rem: isize = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_not: isize = ~a; io.out("{}", r_not);
        let r_neg: isize = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: isize = 7;
        let b: isize = -3;
        let r_add: isize = a + b; io.out("{}", r_add);
        let r_sub: isize = a - b; io.out("{}", r_sub);
        let r_mul: isize = a * b; io.out("{}", r_mul);
        let r_and: isize = a & b; io.out("{}", r_and);
        let r_or: isize = a | b; io.out("{}", r_or);
        let r_xor: isize = a ^ b; io.out("{}", r_xor);
        let r_div: isize = a / b; io.out("{}", r_div);
        let r_rem: isize = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: isize = a << 1; io.out("{}", r_shl1);
        let r_shr1: isize = a >> 1; io.out("{}", r_shr1);
        let r_shl3: isize = a << 3; io.out("{}", r_shl3);
        let r_shr3: isize = a >> 3; io.out("{}", r_shr3);
        let r_shl63: isize = a << 63; io.out("{}", r_shl63);
        let r_shr63: isize = a >> 63; io.out("{}", r_shr63);
        let r_not: isize = ~a; io.out("{}", r_not);
        let r_neg: isize = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: isize = -7;
        let b: isize = -3;
        let r_add: isize = a + b; io.out("{}", r_add);
        let r_sub: isize = a - b; io.out("{}", r_sub);
        let r_mul: isize = a * b; io.out("{}", r_mul);
        let r_and: isize = a & b; io.out("{}", r_and);
        let r_or: isize = a | b; io.out("{}", r_or);
        let r_xor: isize = a ^ b; io.out("{}", r_xor);
        let r_div: isize = a / b; io.out("{}", r_div);
        let r_rem: isize = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_not: isize = ~a; io.out("{}", r_not);
        let r_neg: isize = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: isize = 4611686018427387906;
        let b: isize = 9;
        let r_add: isize = a + b; io.out("{}", r_add);
        let r_sub: isize = a - b; io.out("{}", r_sub);
        let r_mul: isize = a * b; io.out("{}", r_mul);
        let r_and: isize = a & b; io.out("{}", r_and);
        let r_or: isize = a | b; io.out("{}", r_or);
        let r_xor: isize = a ^ b; io.out("{}", r_xor);
        let r_div: isize = a / b; io.out("{}", r_div);
        let r_rem: isize = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: isize = a << 1; io.out("{}", r_shl1);
        let r_shr1: isize = a >> 1; io.out("{}", r_shr1);
        let r_shl3: isize = a << 3; io.out("{}", r_shl3);
        let r_shr3: isize = a >> 3; io.out("{}", r_shr3);
        let r_shl63: isize = a << 63; io.out("{}", r_shl63);
        let r_shr63: isize = a >> 63; io.out("{}", r_shr63);
        let r_not: isize = ~a; io.out("{}", r_not);
        let r_neg: isize = 0 - a; io.out("{}", r_neg);
    }
    return 0;
}
