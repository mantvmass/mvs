// GENERATED by scripts/gen_ops_diff.py: every operator on u16
// The C file next to it is the reference; difftest.sh requires identical output.
import { io } from "std";

func main() -> i8 {
    {
        let a: u16 = 65535;
        let b: u16 = 1;
        let r_add: u16 = a + b; io.out("{}", r_add);
        let r_sub: u16 = a - b; io.out("{}", r_sub);
        let r_mul: u16 = a * b; io.out("{}", r_mul);
        let r_and: u16 = a & b; io.out("{}", r_and);
        let r_or: u16 = a | b; io.out("{}", r_or);
        let r_xor: u16 = a ^ b; io.out("{}", r_xor);
        let r_div: u16 = a / b; io.out("{}", r_div);
        let r_rem: u16 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: u16 = a << 1; io.out("{}", r_shl1);
        let r_shr1: u16 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: u16 = a << 3; io.out("{}", r_shl3);
        let r_shr3: u16 = a >> 3; io.out("{}", r_shr3);
        let r_shl15: u16 = a << 15; io.out("{}", r_shl15);
        let r_shr15: u16 = a >> 15; io.out("{}", r_shr15);
        let r_not: u16 = ~a; io.out("{}", r_not);
    }
    {
        let a: u16 = 0;
        let b: u16 = 5;
        let r_add: u16 = a + b; io.out("{}", r_add);
        let r_sub: u16 = a - b; io.out("{}", r_sub);
        let r_mul: u16 = a * b; io.out("{}", r_mul);
        let r_and: u16 = a & b; io.out("{}", r_and);
        let r_or: u16 = a | b; io.out("{}", r_or);
        let r_xor: u16 = a ^ b; io.out("{}", r_xor);
        let r_div: u16 = a / b; io.out("{}", r_div);
        let r_rem: u16 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: u16 = a << 1; io.out("{}", r_shl1);
        let r_shr1: u16 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: u16 = a << 3; io.out("{}", r_shl3);
        let r_shr3: u16 = a >> 3; io.out("{}", r_shr3);
        let r_shl15: u16 = a << 15; io.out("{}", r_shl15);
        let r_shr15: u16 = a >> 15; io.out("{}", r_shr15);
        let r_not: u16 = ~a; io.out("{}", r_not);
    }
    {
        let a: u16 = 65535;
        let b: u16 = 3;
        let r_add: u16 = a + b; io.out("{}", r_add);
        let r_sub: u16 = a - b; io.out("{}", r_sub);
        let r_mul: u16 = a * b; io.out("{}", r_mul);
        let r_and: u16 = a & b; io.out("{}", r_and);
        let r_or: u16 = a | b; io.out("{}", r_or);
        let r_xor: u16 = a ^ b; io.out("{}", r_xor);
        let r_div: u16 = a / b; io.out("{}", r_div);
        let r_rem: u16 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: u16 = a << 1; io.out("{}", r_shl1);
        let r_shr1: u16 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: u16 = a << 3; io.out("{}", r_shl3);
        let r_shr3: u16 = a >> 3; io.out("{}", r_shr3);
        let r_shl15: u16 = a << 15; io.out("{}", r_shl15);
        let r_shr15: u16 = a >> 15; io.out("{}", r_shr15);
        let r_not: u16 = ~a; io.out("{}", r_not);
    }
    {
        let a: u16 = 32770;
        let b: u16 = 9;
        let r_add: u16 = a + b; io.out("{}", r_add);
        let r_sub: u16 = a - b; io.out("{}", r_sub);
        let r_mul: u16 = a * b; io.out("{}", r_mul);
        let r_and: u16 = a & b; io.out("{}", r_and);
        let r_or: u16 = a | b; io.out("{}", r_or);
        let r_xor: u16 = a ^ b; io.out("{}", r_xor);
        let r_div: u16 = a / b; io.out("{}", r_div);
        let r_rem: u16 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: u16 = a << 1; io.out("{}", r_shl1);
        let r_shr1: u16 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: u16 = a << 3; io.out("{}", r_shl3);
        let r_shr3: u16 = a >> 3; io.out("{}", r_shr3);
        let r_shl15: u16 = a << 15; io.out("{}", r_shl15);
        let r_shr15: u16 = a >> 15; io.out("{}", r_shr15);
        let r_not: u16 = ~a; io.out("{}", r_not);
    }
    {
        let a: u16 = 7;
        let b: u16 = 3;
        let r_add: u16 = a + b; io.out("{}", r_add);
        let r_sub: u16 = a - b; io.out("{}", r_sub);
        let r_mul: u16 = a * b; io.out("{}", r_mul);
        let r_and: u16 = a & b; io.out("{}", r_and);
        let r_or: u16 = a | b; io.out("{}", r_or);
        let r_xor: u16 = a ^ b; io.out("{}", r_xor);
        let r_div: u16 = a / b; io.out("{}", r_div);
        let r_rem: u16 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: u16 = a << 1; io.out("{}", r_shl1);
        let r_shr1: u16 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: u16 = a << 3; io.out("{}", r_shl3);
        let r_shr3: u16 = a >> 3; io.out("{}", r_shr3);
        let r_shl15: u16 = a << 15; io.out("{}", r_shl15);
        let r_shr15: u16 = a >> 15; io.out("{}", r_shr15);
        let r_not: u16 = ~a; io.out("{}", r_not);
    }
    {
        let a: u16 = 200;
        let b: u16 = 7;
        let r_add: u16 = a + b; io.out("{}", r_add);
        let r_sub: u16 = a - b; io.out("{}", r_sub);
        let r_mul: u16 = a * b; io.out("{}", r_mul);
        let r_and: u16 = a & b; io.out("{}", r_and);
        let r_or: u16 = a | b; io.out("{}", r_or);
        let r_xor: u16 = a ^ b; io.out("{}", r_xor);
        let r_div: u16 = a / b; io.out("{}", r_div);
        let r_rem: u16 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: u16 = a << 1; io.out("{}", r_shl1);
        let r_shr1: u16 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: u16 = a << 3; io.out("{}", r_shl3);
        let r_shr3: u16 = a >> 3; io.out("{}", r_shr3);
        let r_shl15: u16 = a << 15; io.out("{}", r_shl15);
        let r_shr15: u16 = a >> 15; io.out("{}", r_shr15);
        let r_not: u16 = ~a; io.out("{}", r_not);
    }
    return 0;
}
