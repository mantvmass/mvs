// GENERATED by scripts/gen_ops_diff.py: every operator on i16
// The C file next to it is the reference; difftest.sh requires identical output.
import { io } from "std";

func main() -> i8 {
    {
        let a: i16 = 32767;
        let b: i16 = 1;
        let r_add: i16 = a + b; io.out("{}", r_add);
        let r_sub: i16 = a - b; io.out("{}", r_sub);
        let r_mul: i16 = a * b; io.out("{}", r_mul);
        let r_and: i16 = a & b; io.out("{}", r_and);
        let r_or: i16 = a | b; io.out("{}", r_or);
        let r_xor: i16 = a ^ b; io.out("{}", r_xor);
        let r_div: i16 = a / b; io.out("{}", r_div);
        let r_rem: i16 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: i16 = a << 1; io.out("{}", r_shl1);
        let r_shr1: i16 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: i16 = a << 3; io.out("{}", r_shl3);
        let r_shr3: i16 = a >> 3; io.out("{}", r_shr3);
        let r_shl15: i16 = a << 15; io.out("{}", r_shl15);
        let r_shr15: i16 = a >> 15; io.out("{}", r_shr15);
        let r_not: i16 = ~a; io.out("{}", r_not);
        let r_neg: i16 = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: i16 = -32768;
        let b: i16 = 1;
        let r_add: i16 = a + b; io.out("{}", r_add);
        let r_sub: i16 = a - b; io.out("{}", r_sub);
        let r_mul: i16 = a * b; io.out("{}", r_mul);
        let r_and: i16 = a & b; io.out("{}", r_and);
        let r_or: i16 = a | b; io.out("{}", r_or);
        let r_xor: i16 = a ^ b; io.out("{}", r_xor);
        let r_div: i16 = a / b; io.out("{}", r_div);
        let r_rem: i16 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_not: i16 = ~a; io.out("{}", r_not);
        let r_neg: i16 = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: i16 = 32767;
        let b: i16 = 3;
        let r_add: i16 = a + b; io.out("{}", r_add);
        let r_sub: i16 = a - b; io.out("{}", r_sub);
        let r_mul: i16 = a * b; io.out("{}", r_mul);
        let r_and: i16 = a & b; io.out("{}", r_and);
        let r_or: i16 = a | b; io.out("{}", r_or);
        let r_xor: i16 = a ^ b; io.out("{}", r_xor);
        let r_div: i16 = a / b; io.out("{}", r_div);
        let r_rem: i16 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: i16 = a << 1; io.out("{}", r_shl1);
        let r_shr1: i16 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: i16 = a << 3; io.out("{}", r_shl3);
        let r_shr3: i16 = a >> 3; io.out("{}", r_shr3);
        let r_shl15: i16 = a << 15; io.out("{}", r_shl15);
        let r_shr15: i16 = a >> 15; io.out("{}", r_shr15);
        let r_not: i16 = ~a; io.out("{}", r_not);
        let r_neg: i16 = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: i16 = -32768;
        let b: i16 = 7;
        let r_add: i16 = a + b; io.out("{}", r_add);
        let r_sub: i16 = a - b; io.out("{}", r_sub);
        let r_mul: i16 = a * b; io.out("{}", r_mul);
        let r_and: i16 = a & b; io.out("{}", r_and);
        let r_or: i16 = a | b; io.out("{}", r_or);
        let r_xor: i16 = a ^ b; io.out("{}", r_xor);
        let r_div: i16 = a / b; io.out("{}", r_div);
        let r_rem: i16 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_not: i16 = ~a; io.out("{}", r_not);
        let r_neg: i16 = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: i16 = 0;
        let b: i16 = 5;
        let r_add: i16 = a + b; io.out("{}", r_add);
        let r_sub: i16 = a - b; io.out("{}", r_sub);
        let r_mul: i16 = a * b; io.out("{}", r_mul);
        let r_and: i16 = a & b; io.out("{}", r_and);
        let r_or: i16 = a | b; io.out("{}", r_or);
        let r_xor: i16 = a ^ b; io.out("{}", r_xor);
        let r_div: i16 = a / b; io.out("{}", r_div);
        let r_rem: i16 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: i16 = a << 1; io.out("{}", r_shl1);
        let r_shr1: i16 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: i16 = a << 3; io.out("{}", r_shl3);
        let r_shr3: i16 = a >> 3; io.out("{}", r_shr3);
        let r_shl15: i16 = a << 15; io.out("{}", r_shl15);
        let r_shr15: i16 = a >> 15; io.out("{}", r_shr15);
        let r_not: i16 = ~a; io.out("{}", r_not);
        let r_neg: i16 = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: i16 = -1;
        let b: i16 = -1;
        let r_add: i16 = a + b; io.out("{}", r_add);
        let r_sub: i16 = a - b; io.out("{}", r_sub);
        let r_mul: i16 = a * b; io.out("{}", r_mul);
        let r_and: i16 = a & b; io.out("{}", r_and);
        let r_or: i16 = a | b; io.out("{}", r_or);
        let r_xor: i16 = a ^ b; io.out("{}", r_xor);
        let r_div: i16 = a / b; io.out("{}", r_div);
        let r_rem: i16 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_not: i16 = ~a; io.out("{}", r_not);
        let r_neg: i16 = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: i16 = 7;
        let b: i16 = 3;
        let r_add: i16 = a + b; io.out("{}", r_add);
        let r_sub: i16 = a - b; io.out("{}", r_sub);
        let r_mul: i16 = a * b; io.out("{}", r_mul);
        let r_and: i16 = a & b; io.out("{}", r_and);
        let r_or: i16 = a | b; io.out("{}", r_or);
        let r_xor: i16 = a ^ b; io.out("{}", r_xor);
        let r_div: i16 = a / b; io.out("{}", r_div);
        let r_rem: i16 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: i16 = a << 1; io.out("{}", r_shl1);
        let r_shr1: i16 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: i16 = a << 3; io.out("{}", r_shl3);
        let r_shr3: i16 = a >> 3; io.out("{}", r_shr3);
        let r_shl15: i16 = a << 15; io.out("{}", r_shl15);
        let r_shr15: i16 = a >> 15; io.out("{}", r_shr15);
        let r_not: i16 = ~a; io.out("{}", r_not);
        let r_neg: i16 = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: i16 = -7;
        let b: i16 = 3;
        let r_add: i16 = a + b; io.out("{}", r_add);
        let r_sub: i16 = a - b; io.out("{}", r_sub);
        let r_mul: i16 = a * b; io.out("{}", r_mul);
        let r_and: i16 = a & b; io.out("{}", r_and);
        let r_or: i16 = a | b; io.out("{}", r_or);
        let r_xor: i16 = a ^ b; io.out("{}", r_xor);
        let r_div: i16 = a / b; io.out("{}", r_div);
        let r_rem: i16 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_not: i16 = ~a; io.out("{}", r_not);
        let r_neg: i16 = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: i16 = 7;
        let b: i16 = -3;
        let r_add: i16 = a + b; io.out("{}", r_add);
        let r_sub: i16 = a - b; io.out("{}", r_sub);
        let r_mul: i16 = a * b; io.out("{}", r_mul);
        let r_and: i16 = a & b; io.out("{}", r_and);
        let r_or: i16 = a | b; io.out("{}", r_or);
        let r_xor: i16 = a ^ b; io.out("{}", r_xor);
        let r_div: i16 = a / b; io.out("{}", r_div);
        let r_rem: i16 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: i16 = a << 1; io.out("{}", r_shl1);
        let r_shr1: i16 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: i16 = a << 3; io.out("{}", r_shl3);
        let r_shr3: i16 = a >> 3; io.out("{}", r_shr3);
        let r_shl15: i16 = a << 15; io.out("{}", r_shl15);
        let r_shr15: i16 = a >> 15; io.out("{}", r_shr15);
        let r_not: i16 = ~a; io.out("{}", r_not);
        let r_neg: i16 = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: i16 = -7;
        let b: i16 = -3;
        let r_add: i16 = a + b; io.out("{}", r_add);
        let r_sub: i16 = a - b; io.out("{}", r_sub);
        let r_mul: i16 = a * b; io.out("{}", r_mul);
        let r_and: i16 = a & b; io.out("{}", r_and);
        let r_or: i16 = a | b; io.out("{}", r_or);
        let r_xor: i16 = a ^ b; io.out("{}", r_xor);
        let r_div: i16 = a / b; io.out("{}", r_div);
        let r_rem: i16 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_not: i16 = ~a; io.out("{}", r_not);
        let r_neg: i16 = 0 - a; io.out("{}", r_neg);
    }
    {
        let a: i16 = 16386;
        let b: i16 = 9;
        let r_add: i16 = a + b; io.out("{}", r_add);
        let r_sub: i16 = a - b; io.out("{}", r_sub);
        let r_mul: i16 = a * b; io.out("{}", r_mul);
        let r_and: i16 = a & b; io.out("{}", r_and);
        let r_or: i16 = a | b; io.out("{}", r_or);
        let r_xor: i16 = a ^ b; io.out("{}", r_xor);
        let r_div: i16 = a / b; io.out("{}", r_div);
        let r_rem: i16 = a % b; io.out("{}", r_rem);
        let r_lt: bool = a < b; io.out("{}", r_lt as i32);
        let r_le: bool = a <= b; io.out("{}", r_le as i32);
        let r_gt: bool = a > b; io.out("{}", r_gt as i32);
        let r_ge: bool = a >= b; io.out("{}", r_ge as i32);
        let r_eq: bool = a == b; io.out("{}", r_eq as i32);
        let r_ne: bool = a != b; io.out("{}", r_ne as i32);
        let r_shl1: i16 = a << 1; io.out("{}", r_shl1);
        let r_shr1: i16 = a >> 1; io.out("{}", r_shr1);
        let r_shl3: i16 = a << 3; io.out("{}", r_shl3);
        let r_shr3: i16 = a >> 3; io.out("{}", r_shr3);
        let r_shl15: i16 = a << 15; io.out("{}", r_shl15);
        let r_shr15: i16 = a >> 15; io.out("{}", r_shr15);
        let r_not: i16 = ~a; io.out("{}", r_not);
        let r_neg: i16 = 0 - a; io.out("{}", r_neg);
    }
    return 0;
}
